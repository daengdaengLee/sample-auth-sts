// Package domain 은 README 의 헥사고날 설계에서 "도메인 코어"에 해당한다. PoP 위임의
// 신뢰 판단 결정 논리(서버 바인딩 검증, 전달 요청 형태 검증, 신선도 검증, 허용 신원
// 대조, 자격 발급 결정)만 담고, 전송 수단이나 STS 호출 방식, 자격 발급 방식 같은 기술
// 세부는 알지 못한다. 코어는 아래의 인바운드/아웃바운드 포트 추상에만 의존하며, 실제
// 기술은 어댑터가 그 포트를 구현해 채운다.
//
// 이 패키지는 표준 라이브러리에만 의존한다(AWS SDK, JWT 등 외부 의존 없음). 특히 전송
// 기술에서 코어를 떼어 두기 위해 net/http 를 import 하지 않고, 메서드/헤더 같은 값은
// 문자열/맵으로 받는다.
package domain
